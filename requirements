# ============================================================
# 🐄 Bovine SNP Platform v4.0 — Dépendances complètes
# Python 3.9 – 3.12
# ============================================================

# ---------- CORE ----------
numpy>=1.24,<2.3
pandas>=2.0,<3.0
scipy>=1.10,<1.15
scikit-learn>=1.3,<1.6

# ---------- WEB UI ----------
streamlit>=1.30,<2.0
streamlit-option-menu>=0.3.6      # optionnel : menu stylé
watchdog>=3.0                      # hot-reload (dev)

# ---------- VISUALISATION ----------
plotly>=5.18,<6.0
kaleido>=0.2.1                     # export PNG statique de Plotly

# ---------- RAPPORT PDF ----------
matplotlib>=3.7,<4.0

# ---------- IA / LLM (Groq gratuit, Ollama local) ----------
requests>=2.31,<3.0
httpx>=0.27,<1.0                   # fallback HTTP pour Groq

# ---------- EXPORT / IO ----------
openpyxl>=3.1,<4.0                 # export Excel (optionnel)
pyarrow>=14.0                      # parquet (cache rapide)

# ---------- DEV / QUALITÉ ----------
pytest>=7.4
black>=24.0
ruff>=0.4
